export interface ThemeType {
  spacing: {
    xs: string;
    sm: string;
    md: string;
    lg: string;
    xl: string;
  };
  colors: {
    primary: string;
    secondary: string;
    grey: string;
    lightGrey: string;
    darkGrey: string;
  };
  fonts: {
    heading: string;
    body: string;
  };
};